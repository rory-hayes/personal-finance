import React from 'react';
import PlaceholderCard from './PlaceholderCard';

const BudgetTrackingCard: React.FC<{ card: any; financeData: any }> = ({ card }) => {
  return <PlaceholderCard card={card} />;
};

export default BudgetTrackingCard; 