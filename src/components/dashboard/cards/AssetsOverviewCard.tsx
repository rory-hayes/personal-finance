import React from 'react';
import PlaceholderCard from './PlaceholderCard';

const AssetsOverviewCard: React.FC<{ card: any; financeData: any }> = ({ card }) => {
  return <PlaceholderCard card={card} />;
};

export default AssetsOverviewCard; 